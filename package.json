{
  "name": "parvina",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "validate:data": "node scripts/validate-data.mjs",
    "generate:seo": "node scripts/generate-seo-pages.mjs",
    "validate:seo": "node scripts/validate-seo.mjs",
    "build": "npm run generate:seo && npm run validate:seo && vite build --config src/vite.config.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.3.6"
  }
}
